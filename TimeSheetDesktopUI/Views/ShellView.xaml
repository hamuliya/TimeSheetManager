<Window x:Class="TimeSheetDesktopUI.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TimeSheetDesktopUI.Views"
        
        mc:Ignorable="d"
        Title="ShellView" Height="800" Width="1000" FontSize="18">
    <DockPanel>
        <Menu DockPanel.Dock="Top" >
            <MenuItem Header="_TimeSheet">
                <MenuItem x:Name="DailyTimeSheet" Header="Daily Time Sheet"/>
                <MenuItem x:Name="DailyTimeSheetSearch" Header="Search"/>
            </MenuItem>

            <MenuItem Header="Info">
                <MenuItem Header="User" x:Name="UserInfo"/>
                <MenuItem Header="Client" x:Name="ClientInfo"/>
                <MenuItem Header="Plant" x:Name="PlantInfo"/>
            </MenuItem>
        </Menu>
        <Grid>
            <ContentControl x:Name="ActiveItem" Margin="20 5 20 20">
            </ContentControl>
        </Grid>
    </DockPanel>
</Window>


